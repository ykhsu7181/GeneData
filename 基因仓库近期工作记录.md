# 项目二：基因仓库近期工作记录

## 目录

1. 项目总览
2. 数据管理与存储
   2.1 原始数据
   2.2 元数据与层级模型
3. 代码与软件环境
4. 实验过程与实验结果
   4.1 Plan 阶段：数据对象与改造目标确认
   4.2 模型构建与规范确认
   4.3 开发阶段
   4.4 阶段性结果汇总
5. 失败与问题记录
6. 结果与讨论
7. 合规与数据共享说明
8. 后续工作

## 1 项目总览

项目名称：基因仓库管理系统近期迭代与数据层级整理

记录日期：2026 年 4 月 12 日

本次记录时间范围：2026 年 3 月 20 日至 2026 年 4 月 10 日

项目类型：数据库整理、文件管理流程改造与 Web 平台功能迭代

本阶段工作目标：围绕现有基因仓库项目，将原先偏“文件列表式”的管理方式，逐步整理为以 accession 为核心、assembly 和 annotation 为层级扩展的数据组织方式；同时补齐数据导入、数据重扫、详情查询、前端卡片页展示和本地开发文档，提升后续数据管理、页面跳转与模块扩展的一致性。

本阶段主要工作内容概述如下：

1. 新增 accession 主表，并将已有 GenomeFile 记录逐步关联到 accession。
2. 建立 assembly 与 annotation 两级模型，形成 accession -> assembly -> annotation 的层级关系。
3. 为已有文件数据补建默认 assembly/default annotation，保证旧数据在新结构下仍可继续访问。
4. 补充 accession 的国家、地区、经纬度、亚群、测序来源等元数据导入能力。
5. 改造 accession 详情接口与前端 accession 卡片页，使页面能够展示层级结构、默认上下文和资源入口。
6. 增加文件扫描、数据重扫、文件上传下载删除、批量删除 accession 等后台管理能力。
7. 补写开发环境启动流程文档，降低后续维护与交接成本。

预期效果：将基因仓库从“单文件可下载”提升为“围绕 accession 进行组织和导航”的平台形态，为后续增加多版本组装、多版本注释、图谱定位、变异块展示和更多数据类型接入打下基础。

## 2 数据管理与存储

### 2.1 原始数据

当前项目中的原始文件主要分布在 `manual_files/` 与 `raw_data/` 目录下。其中 `manual_files/` 作为人工整理后的标准化输入目录，已经承担了后台扫描、数据管理、上传下载等功能的主要文件入口；`raw_data/` 仍保留部分原始分析结果、下载结果和脚本运行产物，用于分析复现与补充核对。

本阶段涉及的主要数据类型包括：

1. 基因组序列文件，如 `genome.{accession}.fasta`、`genome.{accession}.fa`。
2. 基因注释文件，如 `annotation.{accession}.gff`、`annotation.{accession}.gff3`。
3. 转录组数据包，如 `transcriptome.root.{accession}.tar.gz`、`transcriptome.leaf.{accession}.tar.gz` 等。
4. Codon、TEs、centromere、coreBlocks、variableBlocks、miRNA、tRNA、rRNA 等功能或结构相关文件。
5. accession 补充信息文件 `supplymentary_data.txt`，用于导入亚群、测序来源、国家地区和经纬度等元数据。

本阶段对文件命名规则进行了进一步固化，后台扫描逻辑按照以下命名方式识别文件类别与 accession：

1. 常规文件：`category.accession.ext`
2. 转录组文件：`transcriptome.tissue.accession.ext`

该命名规范直接服务于 `scan_files.py`、`link_genomefiles_to_accessions.py` 以及前端数据管理页面，是本轮整理工作的关键前提。

### 2.2 元数据与层级模型

本阶段的核心变化不是单纯增加文件，而是重组元数据结构。

后端模型目前以以下对象为主：

1. `Accession`
   记录 accession、sub_population、seq_data、country、region、longitude、latitude、description 等字段。
2. `Assembly`
   作为 accession 下一级对象，支持一个 accession 对应多个 assembly，并允许指定默认 assembly。
3. `Annotation`
   作为 assembly 下一级对象，支持一个 assembly 对应多个 annotation，并允许指定默认 annotation。
4. `GenomeFile`
   继续作为文件实体，但新增 `accession`、`assembly`、`annotation` 外键，使文件能够挂接到不同层级。

在文件作用域划分上，本阶段进一步定义了三类 scope：

1. assembly-level：如 genome、coreBlocks、variableBlocks、TEs、miRNA、tRNA、rRNA、centromere。
2. annotation-level：如 annotation。
3. compatibility-assembly：如 transcriptome、codon、other 等暂未完全明确业务归属、但先挂接到 assembly 以保持兼容的数据。

这一层级化设计使前端可以不再只依赖文件名判断上下文，而是通过 accession、assembly、annotation 三类 ID 进行资源定位与页面跳转。

## 3 代码与软件环境

本阶段实际使用的开发环境以本地 Windows 开发环境为主，后端与前端分别独立启动。

后端技术栈：

1. Django 3.2.x
2. Django REST Framework
3. PyMySQL
4. Biopython
5. 自定义 management commands 用于数据导入、数据关联、目录扫描与示例层级数据构造

前端技术栈：

1. Vue 3
2. Vue Router 4
3. Axios
4. Element Plus
5. ECharts
6. D3
7. xlsx

项目中的本地开发流程已经在 `开发环境启动流程.md` 中补充整理，当前推荐启动方式为：

1. 进入 `django2/`，创建或激活虚拟环境后运行 `python manage.py runserver 0.0.0.0:2025`
2. 进入 `vue_project/`，执行 `yarn serve`

该文档同时补充了数据库创建、依赖安装、迁移、`/gd/api/debug/` 与 `/docs/` 检查步骤，已经可以作为后续交接和环境复现的基础说明。

## 4 实验过程与实验结果

### 4.1 Plan 阶段：数据对象与改造目标确认

在项目进入最近一轮开发之前，系统已经具备基础的文件展示、下载和部分分析页面，但数据组织方式仍以 `GenomeFile` 为中心，页面切换更多依赖 accession 名称与文件类别直接拼接，难以支撑后续的多 assembly、多 annotation 场景。

因此，本阶段首先明确了以下改造目标：

1. 将 accession 作为数据组织的第一层核心对象。
2. 将 assembly 和 annotation 作为 accession 下的层级扩展，而不是继续把所有文件平铺在同一个文件表中。
3. 尽量不推翻现有页面与接口，而是在兼容旧数据的前提下逐步过渡。
4. 通过默认 assembly/default annotation 的方式，为既有数据补足层级结构。
5. 让前端详情页、资源页和总览页都能感知当前上下文，而不是仅靠 accession 字符串跳转。

本阶段确认后，后续改造均围绕“兼容旧数据、建立新层级、前后端同步感知上下文”三条主线推进。

### 4.2 模型构建与规范确认

这一阶段的重点是把设计目标落实为数据库结构与导入规范。

首先，在 2026 年 3 月 20 日开始的迁移中新增了 `Accession` 模型，并在 `GenomeFile` 中增加 `accession` 外键，用于把原有文件逐步从 `organism` 字符串关联过渡到显式的 accession 对象。

随后，继续新增 `Assembly` 与 `Annotation` 模型，并通过唯一约束明确：

1. 同一 accession 下 assembly 名称不能重复。
2. 同一 accession 只能有一个默认 assembly。
3. 同一 assembly 下 annotation 名称不能重复。
4. 同一 assembly 只能有一个默认 annotation。

在此基础上，又通过 `0010_backfill_default_assembly_annotation.py` 为已有 accession 和文件记录执行回填：

1. 为每个 accession 建立默认 assembly。
2. 为每个 assembly 建立默认 annotation。
3. 将已有文件按类别挂接到 assembly 或 annotation。
4. 对暂未完全明确归属的数据采用 compatibility-assembly 方式暂存，先保证可访问，再逐步细化。

之后，为 accession 补充 `country` 与 `region` 字段，并更新 `import_accessions.py`，使其能够同时兼容旧版和新版 `supplymentary_data.txt` 格式：

1. 旧格式：Accession、SubPopulation、SeqData、Longitude、Latitude
2. 新格式：Accession、SubPopulation、SeqData、Country、Region、Longitude、Latitude

同时，`0012_alter_genomefile_category_variableblocks.py` 将 `variableBlocks` 纳入正式类别列表，说明近期数据类型已经从 coreBlocks 扩展到 variableBlocks 场景。

### 4.3 开发阶段

在模型稳定后，本阶段转入接口、命令行工具和前端页面的联动开发。

后端部分主要完成了以下工作：

1. 新增 accession 详情接口 `/gd/api/files/accessions/<accession>/`，支持返回 accession 基本信息、assembly 列表、annotation 列表、summary 汇总信息以及兼容旧页面的扁平 files/file_status 视图。
2. 在 `serializers.py` 中实现 `AccessionDetailSerializer`、`AssemblyDetailSerializer`、`AnnotationDetailSerializer` 和 `AccessionGenomeFileSerializer`，使层级数据可以稳定输出给前端。
3. 在 `accession_context.py` 中实现 accession、assembly、annotation 的上下文解析逻辑，为 genome、annotation 等页面按 query 参数选择当前上下文提供统一入口。
4. 编写 `link_genomefiles_to_accessions.py`，根据标准化文件名将已有 `GenomeFile` 记录回链到 accession。
5. 编写 `scan_files.py`，对 `manual_files/` 目录执行扫描，同步新增文件、删除缺失记录，并在可识别时补挂 accession 与默认 assembly。
6. 编写 `import_accessions.py`，从 `supplymentary_data.txt` 导入 accession 元数据。
7. 增加数据管理相关接口，包括 accession 新建、更新、删除、批量删除、文件上传、下载、删除和重扫。

前端部分主要完成了以下工作：

1. 在路由中正式启用 `/accession-card` 页面，并接入 `AccessionCard.vue`。
2. `AccessionCard.vue` 实现了 accession 层级卡片展示，可以展示 assembly 数量、annotation 数量、文件总数、国家地区信息，以及当前选中上下文。
3. 该页面进一步加入了 accession -> assembly -> annotation 的层级结构可视化，并支持切换当前 assembly/annotation。
4. 在 `HomeView.vue` 与 `DataOverviewView.vue` 中，将详情跳转统一为携带 `accession`、`assembly`、`annotation` 参数的上下文跳转，减少页面之间“跳过去但上下文丢失”的问题。
5. `GenomeCard.vue` 等资源页已开始支持从 query 中读取 `accession` 与 `assembly`，从而打开当前 assembly 对应的数据。
6. 管理端页面继续使用 `AdminDataManager.vue`、`AdminFileManager.vue` 承担 accession 与文件管理，并接入重扫、批量下载、批量删除、分类型上传等流程。

为了便于联调与后续维护，还补充了以下辅助工作：

1. 新增 `seed_ir64_demo_hierarchy.py`，用于为 IR64 之类 accession 构造演示用的多 assembly、多 annotation 层级数据，便于前端卡片页预览层级效果。
2. 在 `tests.py` 中补充 accession 层级模型、详情接口和总览接口的测试用例，用于验证默认 assembly/default annotation、旧数据兼容和分页总览统计是否正常。
3. 在 `filemanager/urls.py` 中保留部分旧接口入口和调试入口，降低改造期间对旧页面和旧调用方式的冲击。

### 4.4 阶段性结果汇总

经过本阶段整理，基因仓库已经形成以下阶段性成果：

1. 数据模型已经从单层文件记录扩展为 accession -> assembly -> annotation -> files 的层级结构。
2. 旧数据仍可通过 organism/accession 回退逻辑继续被读取，降低了迁移风险。
3. accession 详情页已经不再只是平铺文件列表，而是能够展示多 assembly、多 annotation 的组织关系。
4. 总览页与卡片页之间的跳转开始携带默认上下文，页面联动更加稳定。
5. accession 元数据已支持国家、地区、经纬度等字段，为后续地理分布展示与来源分析提供了基础。
6. 后台已经具备 accession 级别的数据管理能力，而不仅仅是单文件上传删除。
7. 开发环境启动流程文档已形成，后续部署和交接的门槛有所下降。

## 5 失败与问题记录

在本轮工作中，比较典型的问题主要包括以下几类：

### 5.1 旧字段与新层级并存带来的兼容问题

历史数据主要通过 `GenomeFile.organism` 保存 accession 信息，而新结构要求使用显式的 `accession` 外键。为避免一次性大规模清洗导致页面不可用，本阶段采用“新外键优先、旧字段回退”的兼容策略。该策略虽然保证了系统可继续运行，但也意味着一段时间内代码中需要同时处理两种来源。

### 5.2 文件命名只包含 accession，未直接区分 assembly 与 annotation

现有很多文件仍遵循 `category.accession.ext` 的命名方式，本身并没有提供 assembly 名称或 annotation 名称。因此，在层级迁移早期无法完全自动区分不同层级归属，只能先通过默认 assembly/default annotation 承接旧文件，再逐步细化。

### 5.3 `supplymentary_data.txt` 存在新旧两种格式

在 accession 元数据导入时，国家地区字段是后续补充的，导致旧文件和新文件列数不一致。为避免导入失败，本阶段在导入脚本中显式兼容两种格式，并对经纬度缺失和非法值做空值处理。

### 5.4 磁盘文件与数据库记录容易脱节

此前如果人工改动 `manual_files/` 目录，数据库中的文件记录不会自动同步，容易出现页面可见但磁盘不存在、或磁盘已有但数据库未登记的情况。本阶段通过 `scan_files.py` 和后台“重扫”接口缓解了这一问题，但目录规范仍然需要继续严格执行。

### 5.5 页面跳转过程中的上下文容易丢失

在没有显式传递 `assembly` 和 `annotation` 参数时，前端页面往往只能依赖 accession 名称重新推断上下文，这会导致多 assembly 场景下打开的并不一定是用户刚才点击的那一套数据。本阶段已经在主要入口上补充 query 参数，但仍需继续覆盖更多页面和交互路径。

### 5.6 新数据类型纳入系统时类别定义不统一

近期新加入 `variableBlocks` 后，原有类别列表并未及时同步，导致这一数据类型无法被完整纳入统一扫描和展示流程。4 月 8 日的迁移已将其纳入正式类别，但后续仍需继续核对前后端展示与下载链路是否全部覆盖。

## 6 结果与讨论

本阶段最重要的成果不是单个页面的增加，而是完成了基因仓库数据组织方式的一次结构性调整。系统开始从“围绕文件类别管理”转向“围绕 accession 及其下游层级管理”，这使得后续扩展多 assembly、多 annotation、多来源版本时，不再需要继续在单张文件表里堆叠特殊逻辑。

从实现策略上看，本轮改造没有直接清空旧逻辑重写，而是采用了“先兼容、再收束”的方式推进：先建立新模型和默认层级，再把旧文件逐步映射进去，并继续保留扁平 `files/file_status` 输出给旧页面使用。这种方式牺牲了一部分代码简洁性，但换来了开发连续性和较低的迁移风险，适合当前仍在快速迭代的数据仓库项目。

同时也应看到，当前很多数据仍处于 compatibility-assembly 阶段，说明业务边界还没有完全收束。尤其是 transcriptome、codon 等数据是否应长期挂在 assembly 层，还是未来引入新的实验层级或条件层级，仍然需要结合后续页面需求和数据增长情况进一步讨论。

## 7 合规与数据共享说明

本次记录主要针对本地开发环境和项目内部数据整理流程，不涉及对外正式发布的数据共享策略。

当前数据的主要来源包括：

1. `manual_files/` 中的标准化输入文件；
2. `raw_data/` 中保留的原始或中间结果；
3. `supplymentary_data.txt` 中整理后的 accession 元数据。

本阶段工作的重点是数据结构整理、接口联通和管理流程规范化。后续如需对外共享或公开下载，仍需进一步补充数据来源标注、版本说明、目录清单和权限控制策略。

## 8 后续工作

下一阶段建议继续推进以下工作：

1. 继续细化 transcriptome、codon 等数据的真实业务层级，逐步减少 compatibility-only 映射。
2. 让更多页面统一使用 accession/assembly/annotation 上下文解析逻辑，彻底消除跨页上下文不一致问题。
3. 补充更多扫描规则与命名校验，减少人工放置文件时的目录和命名错误。
4. 将管理员数据管理流程与数据库层级模型进一步打通，例如支持直接维护 assembly 和 annotation。
5. 继续补充测试，尤其是多 assembly、多 annotation、批量删除和重扫后的完整回归测试。
6. 根据当前 `开发环境启动流程.md` 再进一步整理部署、迁移和初始化数据文档，使新成员能更快接手项目。

