# GeneData 开发环境启动流程

开发环境指本地电脑。修改代码后应先在本地完成检查和测试，再提交到远程分支或部署到服务器。

## 1. 前置条件

- Python 和项目虚拟环境可用；Windows 默认使用 `django2/.venv`。
- Node.js 与 npm 可用。
- 本地 MySQL 已启动，并且已经创建 GeneData 使用的开发数据库。
- 不要让开发环境连接生产数据库。

以下命令均在 PowerShell 中执行，项目根目录为：

```powershell
D:\gene_manage_system\gene_manage_system
```

## 2. 配置开发数据库

Django 会管理数据表，但不会代替开发人员安装或启动 MySQL。数据库连接通过当前 PowerShell 会话的环境变量提供：

```powershell
$env:GENEDATA_DB_NAME="gene_manage"
$env:GENEDATA_DB_USER="<开发数据库账号>"
$env:GENEDATA_DB_PASSWORD="<开发数据库密码>"
$env:GENEDATA_DB_HOST="localhost"
$env:GENEDATA_DB_PORT="3306"
```

不要把真实密码写入本文档、源码或 Git。每个新开的 PowerShell 窗口都需要重新设置这些会话环境变量。

首次建立开发数据库，或者代码确实包含待应用迁移时，先确认连接的是开发数据库，再执行：

```powershell
cd D:\gene_manage_system\gene_manage_system\django2
.\.venv\Scripts\python.exe manage.py migrate
```

不要对生产数据库执行本开发流程。

## 3. 启动 Django 后端

打开第一个 PowerShell 窗口，设置第 2 节的数据库环境变量，然后执行：

```powershell
cd D:\gene_manage_system\gene_manage_system\django2

.\.venv\Scripts\python.exe -m pip install -r requirements.txt
.\.venv\Scripts\python.exe manage.py check
.\.venv\Scripts\python.exe manage.py runserver 0.0.0.0:2025
```

后端开发地址为 `http://localhost:2025`。保持该窗口运行。

如果不使用仓库现有的 `.venv`，应先创建并激活独立虚拟环境，再安装 `django2/requirements.txt`，不要把依赖安装到不相关的 Python 环境。

## 4. 启动 Vue 前端

打开第二个 PowerShell 窗口：

```powershell
cd D:\gene_manage_system\gene_manage_system\vue_project

npm ci
npm run serve
```

前端默认地址为 `http://localhost:8080`。开发服务器会把 `/gd/api` 请求代理到 `http://localhost:2025`。

日常启动且依赖没有变化时，可跳过 `npm ci`，直接运行：

```powershell
npm run serve
```

开发时建议始终使用 `http://localhost:8080` 访问前端，不要在同一次登录过程中混用 `localhost` 和 `127.0.0.1`，以免 Session Cookie 或 CSRF Cookie 的站点范围不一致。

## 5. 管理员登录与权限

管理后台入口：

```text
http://localhost:8080/admin/login
```

当前管理写接口使用 Django Session Authentication：

- 必须通过 Django 用户名和密码登录。
- 用户必须满足 `is_staff=True`；`is_superuser=True` 的超级用户也允许访问。
- 匿名用户和 `is_staff=False` 的普通用户不能调用管理写接口。
- 浏览器必须携带 Django Session Cookie 和 CSRF Cookie。
- 前端已配置 `withCredentials`、`csrftoken` 和 `X-CSRFToken`，不再使用 `localStorage` 中的管理员 token 作为后端权限依据。

如果开发数据库还没有管理员账号，在确认连接的是开发数据库后执行：

```powershell
cd D:\gene_manage_system\gene_manage_system\django2
.\.venv\Scripts\python.exe manage.py createsuperuser
```

也可以通过 Django shell 将已有开发用户设置为 staff，但不要在源码中固定管理员用户名或共享密码。

## 6. Session/CSRF 验证

启动前后端后，在浏览器中完成以下检查：

1. 打开 `http://localhost:8080/admin/login`。
2. 使用 `is_staff=True` 的 Django 用户登录。
3. 确认能够进入 `/admin/dashboard`。
4. 验证新增、修改、删除和上传功能可用。
5. 退出后刷新管理页面，确认需要重新登录。
6. 使用匿名用户或普通非 staff 用户，确认管理写操作被拒绝。

如果登录或写操作返回 `403 CSRF Failed`，依次确认：

- 浏览器访问的是 `http://localhost:8080`。
- Django 后端运行在 `http://localhost:2025`。
- 请求经 Vue 开发代理的 `/gd/api` 发出，而不是前端直接跨站调用另一个主机名。
- 浏览器没有阻止 Cookie。
- 没有混用 `localhost` 与 `127.0.0.1`。

不要通过关闭 CSRF、添加 `csrf_exempt` 或恢复前端共享 token 来绕过问题。

## 7. 开发完成后的基础检查

后端：

```powershell
cd D:\gene_manage_system\gene_manage_system\django2
.\.venv\Scripts\python.exe manage.py check
.\.venv\Scripts\python.exe manage.py test files
.\.venv\Scripts\python.exe manage.py makemigrations --check --dry-run files
```

前端：

```powershell
cd D:\gene_manage_system\gene_manage_system\vue_project
npm test
npm run build
```

最后检查工作区，避免把本地数据库凭据、报告、上传文件或其他运行产物提交到 Git：

```powershell
cd D:\gene_manage_system\gene_manage_system
git status --short
git diff --check
```

## 8. 停止服务

分别在后端和前端运行窗口按 `Ctrl+C`。环境变量只设置在当前 PowerShell 会话中，关闭窗口后不会继续生效。
