# 首页 Portal 化阶段 1 基线（2026-09-14）

分支：`feat/homepage-portal-redesign`（从 `fix/views-p0-security` 当前工作区建立）。

改动前验证结果：

| 检查项 | 结果 |
|---|---|
| `npm run build` | PASS；存在既有 bundle 体积警告 |
| `npm test` | PASS；43/43 |
| `npm run lint` | BLOCKED；既有 ESLint 配置报 `No files matching 'tests' were found` |
| `python manage.py check` | PASS |
| `python manage.py test files.tests.api.test_dashboard_api` | BLOCKED；MySQL 拒绝本机 `root` 凭据，无法创建测试库 |

工作区在建分支前已有未提交修改和未跟踪文件，本阶段未重置、清理或覆盖这些内容。

补充说明：生产配置保持 MySQL 不变。为验证代码，在测试进程中临时覆盖为 SQLite 后，Dashboard API 测试 7/7 通过，`files.tests` 完整套件 247 项通过、2 项跳过。
