# 数据库逻辑梳理（第一版，可交老师）

> 状态说明：本文档分为“已从代码确认”和“待业务确认”两类，便于后续快速补全。

## 1. 项目与环境信息

- 项目名称：`gene_manage_system`
- 梳理日期：`2026-05-21`
- 梳理人：`（填写你的名字）`
- 数据库类型与版本：MySQL（版本待确认）
- 连接信息来源：`django2/filemanager/settings.py` 的 `DATABASES`
- 本地库/测试库/生产库区分：
1. 本地：`HOST=localhost`，`DB=gene_manage`
2. 测试/生产：待确认是否有独立配置文件或环境变量覆盖

## 2. 核心业务问题（先写结论）

- 这个系统最核心回答的 3 个业务问题是什么？
1. 一个 `Accession` 下面有哪些组装版本（Assembly）和注释版本（Annotation）？
2. 每个层级下有哪些标准类别的数据文件（Genome、Annotation、Transcriptome 等）？
3. 给定 `accession/assembly/annotation` 参数时，系统应返回哪一份默认或指定的数据文件？

- 哪些表是“主干表”（优先理解）？
1. `accession`
2. `assembly`
3. `annotation`
4. `files_genomefile`

## 3. 数据模型总览（ER 关系）

### 3.1 主关系（已确认）

- 实体 A：`Accession`
- 实体 B：`Assembly`
- 关系：`1:N`
- 关系说明：一个 `Accession` 可以有多个 `Assembly`，且最多一个 `is_default=True`。

- 实体 A：`Assembly`
- 实体 B：`Annotation`
- 关系：`1:N`
- 关系说明：一个 `Assembly` 可以有多个 `Annotation`，且最多一个 `is_default=True`。

- 实体 A：`Annotation / Assembly / Accession`
- 实体 B：`GenomeFile`
- 关系：`1:N`
- 关系说明：`GenomeFile` 通过外键挂到 `accession`，并可选挂到 `assembly`、`annotation`。

### 3.2 当前项目中的关系结论

- 主链路：`Accession -> Assembly -> Annotation -> GenomeFile`
- 兼容链路（旧逻辑）：`文件名规则/organism -> GenomeFile（弱关联）-> 兜底返回`

## 4. 表清单（按优先级）

| 优先级 | 表名 | 用途 | 数据来源 | 被谁使用（接口/页面/任务） |
|---|---|---|---|---|
| P0 | `accession` | accession 主数据（群体、经纬度等） | `import_accessions.py` 导入 `supplymentary_data.txt` | `accession_detail`、overview 查询 |
| P0 | `assembly` | accession 下组装版本层级 | 迁移脚本回填 + 元数据导入 | 默认 assembly 选择、层级详情 |
| P0 | `annotation` | assembly 下注释版本层级 | 迁移脚本回填 + 元数据导入 | 默认 annotation 选择、注释相关接口 |
| P0 | `files_genomefile` | 各类数据文件索引与路径 | `scan_files.py` 扫描目录入库 | 下载、统计、详情接口 |
| P1 | `files_filetype` | 文件扩展名/类型字典 | 扫描脚本动态创建 | 文件列表展示 |
| P1 | `files_filecategory` | 文件类别字典（补充） | 管理维护 | 分类查询 |
| P2 | `files_organism` | organism 辅助表 | 扫描脚本写入 | 历史兼容逻辑 |

## 5. 字段字典（核心表）

### 5.1 表名：`accession`

| 字段名 | 类型 | 主键/外键/索引 | 可空 | 业务含义 | 数据来源 | 更新方式 |
|---|---|---|---|---|---|---|
| `id` | bigint | PK | 否 | 内部主键 | DB 自动 | 自动 |
| `accession` | varchar(50) | 唯一 + 索引 | 否 | accession 编码 | 文件导入 | 脚本 |
| `sub_population` | varchar(100) | - | 是 | 亚群信息 | 文件导入 | 脚本 |
| `seq_data` | varchar(255) | - | 是 | 测序信息 | 文件导入 | 脚本 |
| `country/region` | varchar | - | 是 | 地理来源 | 文件导入 | 脚本 |
| `longitude/latitude` | float | - | 是 | 地理坐标 | 文件导入 | 脚本 |

- 唯一约束：`accession` 唯一
- 默认值规则：无业务默认值（空值允许）
- 软删除/硬删除：当前为硬删除
- 历史兼容字段：`organism` 文本曾承担 accession 语义（在文件表中仍可见）

### 5.2 表名：`assembly`

| 字段名 | 类型 | 主键/外键/索引 | 可空 | 业务含义 | 数据来源 | 更新方式 |
|---|---|---|---|---|---|---|
| `id` | bigint | PK | 否 | 内部主键 | DB 自动 | 自动 |
| `accession_id` | bigint | FK -> accession | 否 | 所属 accession | 迁移/导入 | 脚本 |
| `name` | varchar(255) | 唯一约束组合 | 否 | assembly 名称 | 迁移/元数据 | 脚本 |
| `is_default` | bool | 条件唯一约束 | 否 | 是否默认 assembly | 迁移/维护 | 脚本/API |

- 唯一约束：
1. `(accession, name)` 唯一
2. 每个 accession 最多一个默认 assembly
- 默认值规则：新建默认 `name='default'`（迁移逻辑）
- 软删除/硬删除：硬删除（级联影响 annotation）

### 5.3 表名：`annotation`

| 字段名 | 类型 | 主键/外键/索引 | 可空 | 业务含义 | 数据来源 | 更新方式 |
|---|---|---|---|---|---|---|
| `id` | bigint | PK | 否 | 内部主键 | DB 自动 | 自动 |
| `assembly_id` | bigint | FK -> assembly | 否 | 所属 assembly | 迁移/导入 | 脚本 |
| `name` | varchar(255) | 唯一约束组合 | 否 | annotation 名称 | 迁移/元数据 | 脚本 |
| `is_default` | bool | 条件唯一约束 | 否 | 是否默认 annotation | 迁移/维护 | 脚本/API |

- 唯一约束：
1. `(assembly, name)` 唯一
2. 每个 assembly 最多一个默认 annotation
- 默认值规则：新建默认 `name='default-annotation'`（迁移逻辑）
- 软删除/硬删除：硬删除

### 5.4 表名：`files_genomefile`

| 字段名 | 类型 | 主键/外键/索引 | 可空 | 业务含义 | 数据来源 | 更新方式 |
|---|---|---|---|---|---|---|
| `id` | bigint | PK | 否 | 文件记录主键 | DB 自动 | 自动 |
| `name` | varchar(255) | - | 否 | 文件名 | 目录扫描 | 脚本 |
| `organism` | varchar(50) | - | 否 | 历史兼容标识 | 文件名解析 | 脚本 |
| `category` | varchar(50) | - | 否 | 文件业务类别 | 文件名解析 | 脚本 |
| `file_path` | varchar(500) | - | 否 | 文件绝对路径 | 扫描目录 | 脚本 |
| `accession_id` | bigint | FK -> accession | 是 | 主链路关联 | 扫描/补链路 | 脚本 |
| `assembly_id` | bigint | FK -> assembly | 是 | assembly 层级关联 | 迁移/补链路 | 脚本 |
| `annotation_id` | bigint | FK -> annotation | 是 | annotation 层级关联 | 迁移/补链路 | 脚本 |

- 唯一约束：暂无强唯一（待评估是否需要）
- 默认值规则：`size=0`；外键可空
- 软删除/硬删除：硬删除
- 历史兼容字段：`organism` + 文件名推断仍被兜底逻辑使用

## 6. 数据流与血缘（从文件/上游到库到接口）

### 6.1 入库链路（已确认）

| 步骤 | 输入 | 处理脚本/命令 | 输出表 | 备注 |
|---|---|---|---|---|
| 1 | `manual_files/supplymentary_data.txt` | `python manage.py import_accessions` | `accession` | 写入/更新 accession 主数据 |
| 2 | `manual_files` 目录文件 | `python manage.py scan_files` | `files_genomefile`、`files_filetype`、`files_organism` | 解析文件名类别和 organism |
| 3 | 已入库文件记录 + accession | `python manage.py link_genomefiles_to_accessions` | 更新 `files_genomefile.accession_id` | 补全旧数据到新外键 |
| 4 | `assembly_metadata.tsv`、`annotation_metadata.tsv` | `python manage.py import_accession_hierarchy_metadata` | `assembly/annotation` 元信息字段 | 展示字段补充 |

### 6.2 消费链路（已确认主路径）

| 消费方（页面/API/报表） | 读取表 | 关键过滤条件 | 返回字段 | 业务口径 |
|---|---|---|---|---|
| accession 详情接口 | accession + assembly + annotation + genomefile | accession 编码、默认 assembly/annotation 选择 | 文件清单、层级详情、统计 | 优先新外键链路，缺失时走旧兜底 |
| overview 接口 | accession + 关联 files | 搜索词、群体过滤 | 各类别文件存在性与计数 | 基于默认层级汇总 |
| 注释数据接口 | genomefile/磁盘文件 | organism/accession/assembly_id/annotation_id | 注释分页结果 | 先定位 annotation 文件，找不到回退命名规则 |

## 7. 默认规则与业务口径（重点）

- 默认主记录规则：
1. 每个 accession 最多一个 `default assembly`
2. 每个 assembly 最多一个 `default annotation`

- 同一业务对象多版本选择逻辑：
1. 优先使用请求显式传入的 `assembly_id` / `annotation_id`
2. 否则选 `is_default=True`
3. 若无默认，则退化为第一个可用记录

- 文件命名与分类规则（示例）：
1. `genome.<accession>.<ext>`
2. `annotation.<accession>.gff(.gz)`
3. `transcriptome.<tissue>.<accession>.tar.gz`

- 同名/重复数据处理规则：
1. 扫描脚本按类别+organism 查已有记录后更新路径
2. 是否允许同类多文件并存：待业务确认

-层级归属以业务规则为准。
导入元数据是主依据，脚本/文件名只做辅助。
入库时必须落明确外键：至少 assembly_id，需要注释层时再写 annotation_id。
接口层只读取，不再承担“猜归属”的责任。

## 8. 兼容逻辑（新旧并存）

- 新逻辑入口：基于 `accession/assembly/annotation` 外键关系查询
- 旧逻辑入口：基于 `organism` 字段和文件名模式匹配
- 兜底规则：新链路缺失时，回退到 `organism` 或 `annotation.<organism>.gff` 规则
- 风险：
1. 新旧逻辑可能返回不同文件版本
2. 统计口径可能因兜底路径不同而波动

## 9. 数据质量检查清单（SQL + 结果）

### 9.1 规模检查

```sql
select count(*) from accession;
select count(*) from assembly;
select count(*) from annotation;
select count(*) from files_genomefile;
```

### 9.2 关系完整性检查

```sql
select count(*) as genomefile_accession_null
from files_genomefile
where accession_id is null;

select accession_id, count(*) as default_assembly_cnt
from assembly
where is_default = 1
group by accession_id
having count(*) > 1;

select assembly_id, count(*) as default_annotation_cnt
from annotation
where is_default = 1
group by assembly_id
having count(*) > 1;
```

### 9.3 结果记录（待执行）

- 发现的问题 1：待填（执行 SQL 后填写）
- 发现的问题 2：待填（执行 SQL 后填写）
- 暂不处理项：待填（与老师确认优先级）

## 10. 接口到表映射（排障必备）

| 接口/函数 | 代码位置 | 读/写 | 关联表 | 关键条件 |
|---|---|---|---|---|
| `accession_detail` | `django2/files/views.py` | 读 | accession/assembly/annotation/genomefile | accession 参数，默认层级选择 |
| `overview`（相关方法） | `django2/files/views.py` | 读 | accession + 关联 files | 搜索、群体过滤、类别聚合 |
| `get_annotation_data`（相关方法） | `django2/files/views.py` | 读 | genomefile + 磁盘文件 | organism/accession/assembly/annotation 参数 |
| `scan_files` 命令 | `django2/files/management/commands/scan_files.py` | 写 | genomefile/filetype/organism | 文件命名规则解析 |

## 11. 变更影响评估模板（改字段前必填）

- 计划变更：`（示例）废弃 organism 兜底，强制走 accession 外键`
- 影响表：`files_genomefile`、`accession`、`assembly`、`annotation`
- 影响接口：`accession_detail`、overview、annotation 相关接口
- 影响脚本/定时任务：`scan_files`、`link_genomefiles_to_accessions`
- 回滚方案：保留旧逻辑开关 + 回滚到上一个稳定版本
- 验证方案：抽样 20 个 accession 对比新旧返回结果一致性

## 12. 交接结论（最终页）

- 我已经确认的事实：
1. 当前主数据模型已升级为层级模型：`Accession -> Assembly -> Annotation -> GenomeFile`
2. 系统仍保留旧的 `organism/文件名` 兼容链路
3. 前端展示正确性高度依赖后端默认层级和文件映射规则

- 我还不确定、需要业务方确认的问题：
1. 是否允许同一 `accession + category` 出现多份有效文件
2. `codon/other/transcriptome.*` 在业务上归属 assembly 还是 annotation
3. 生产环境是否已完全完成旧数据向新外键链路的补齐

- 下周优先处理建议：
1. 执行第 9 节 SQL，填充真实质量结果并出问题清单
2. 对 Top20 accession 做新旧链路返回一致性对比
3. 与老师确认兼容逻辑下线时间表，明确“后端口径锁定”时间点
