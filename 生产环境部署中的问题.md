# Django2 系统重新部署问题排查记录

## 1. 实验背景

### 1.1 项目环境

- 项目名称：`django2`
- 新部署目录：

```text
/home/labuser/rdcheng/gd/django2
```

- 旧版本备份目录：

```text
/home/labuser/rdcheng/gd/django2_20260721
```

- Conda 环境：

```text
django2_env
```

- Python 版本：Python 3.9
- 数据库：MySQL
- 数据库名：`gene_manage`
- 数据库连接地址：`127.0.0.1:3307`

### 1.2 部署目标

重新部署新版 Django 后端代码，同时继续使用旧系统已有 MySQL 数据库中的业务数据，尤其保留：

- Accession
- Assembly
- Annotation
- Accession → Assembly
- Assembly → Annotation

等已有业务关联关系。

---

## 2. 初始部署流程

原部署流程中需要执行：

```bash
python manage.py makemigrations
python manage.py migrate
python manage.py scan_files
```

但在执行过程中连续出现 Django 启动失败、日志路径错误以及数据库配置错误等问题。

---

## 3. 问题一：Django 日志 Handler 配置失败

### 3.1 报错

执行：

```bash
python manage.py makemigrations
python manage.py migrate
python manage.py scan_files
```

出现：

```text
ValueError: Unable to configure handler 'file'
```

进一步 traceback 中出现：

```text
FileNotFoundError: [Errno 2] No such file or directory
```

### 3.2 原因定位

通过搜索日志配置：

```bash
grep -RniE "FileHandler|RotatingFileHandler|TimedRotatingFileHandler|filename.*log"     . --include="*.py"
```

发现：

```text
./filemanager/settings_production.py
```

中存在：

```python
'handlers': {
    'file': {
        'level': 'INFO',
        'class': 'logging.FileHandler',
        'filename': '/var/log/django/filemanager.log',
        'formatter': 'verbose',
    },
}
```

由于 `/var/log/django/` 不存在，Django 在启动时无法初始化 `FileHandler`。

后来重新部署时又发现日志配置指向：

```text
/home/labuser/rdcheng/gd/django/logs/filemanager.log
```

而当前实际项目目录是：

```text
/home/labuser/rdcheng/gd/django2
```

因此日志绝对路径再次失效。

### 3.3 解决方案

将：

```python
'filename': '/home/labuser/rdcheng/gd/django/logs/filemanager.log',
```

修改为：

```python
'filename': '/home/labuser/rdcheng/gd/django2/logs/filemanager.log',
```

并创建日志目录和日志文件：

```bash
cd /home/labuser/rdcheng/gd/django2

mkdir -p logs
touch logs/filemanager.log

chmod 755 logs
chmod 664 logs/filemanager.log
```

测试写入：

```bash
echo "$(date) test" >> logs/filemanager.log
tail -n 3 logs/filemanager.log
```

测试成功后执行：

```bash
python manage.py check
```

结果：

```text
System check identified no issues (0 silenced).
```

### 3.4 建议

不建议长期在配置中写死项目绝对路径。后续可考虑使用 `BASE_DIR` 动态生成日志路径，例如：

```python
'filename': str(BASE_DIR / 'logs' / 'filemanager.log'),
```

从而避免项目目录改名后再次出现相同问题。

---

## 4. 问题二：数据库不存在

### 4.1 报错

日志问题解决后，执行 Django 数据库相关命令时出现：

```text
django.db.utils.OperationalError:
(1049, "Unknown database 'gene_manage'")
```

### 4.2 原因

新项目的 `settings_production.py` 使用了：

```python
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.mysql',
        'NAME': 'gene_manage',
        'USER': 'root',
        'PASSWORD': '******',
        'HOST': 'localhost',
        'PORT': '3306',
    }
}
```

该配置连接的是宿主机 `localhost:3306`，但旧系统实际上并不是通过该端口访问原数据库。

---

## 5. Docker MySQL 排查

### 5.1 普通用户无法访问 Docker

使用 `rdcheng` 执行：

```bash
docker ps -a
```

出现：

```text
permission denied while trying to connect to the Docker daemon socket
```

检查：

```bash
ls -l /var/run/docker.sock
groups
```

结果显示：

```text
/var/run/docker.sock -> root:docker
rdcheng groups -> labuser
```

说明 `rdcheng` 不属于 `docker` 组，因此没有 Docker daemon 访问权限。

### 5.2 使用 root 查看 Docker

切换到 root 后执行：

```bash
docker ps -a --format "table {{.Names}}	{{.Image}}	{{.Status}}	{{.Ports}}"
```

发现两个 MySQL 容器：

```text
nip19k-prod-mysql-1   mysql:8.4.6
xbzhang-mysql57       mysql:5.7
```

其中：

```text
xbzhang-mysql57
0.0.0.0:3307->3306/tcp
```

说明宿主机 `3307` 映射到了该 MySQL 容器内部的 `3306`。

---

## 6. 旧项目数据库配置确认

查看旧备份项目：

```text
/home/labuser/rdcheng/gd/django2_20260721
```

其中 `settings_production.py` 的数据库配置为：

```python
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.mysql',
        'NAME': 'gene_manage',
        'USER': 'gene_user',
        'PASSWORD': '******',
        'HOST': '127.0.0.1',
        'PORT': '3307',
        'OPTIONS': {
            'charset': 'utf8mb4',
        },
    }
}
```

这与 `xbzhang-mysql57` 的宿主机端口映射完全对应。

因此确认旧系统数据库连接方式为：

```text
Django
  ↓
127.0.0.1:3307
  ↓
Docker 端口映射
  ↓
xbzhang-mysql57:3306
  ↓
gene_manage
```

---

## 7. 数据库连接验证

在旧项目中执行：

```bash
cd /home/labuser/rdcheng/gd/django2_20260721
conda activate django2_env

python manage.py shell -c "
from django.db import connection
connection.ensure_connection()
print(connection.settings_dict['HOST'], connection.settings_dict['PORT'])
print('database connection ok')
"
```

输出：

```text
127.0.0.1 3307
database connection ok
```

说明旧项目可以正常连接原数据库。

随后将新项目数据库配置修改为旧项目相同配置，并在新项目中再次测试：

```bash
cd /home/labuser/rdcheng/gd/django2

python manage.py shell -c "
from django.db import connection
connection.ensure_connection()
print(connection.settings_dict['HOST'], connection.settings_dict['PORT'])
print('database connection ok')
"
```

结果同样为：

```text
127.0.0.1 3307
database connection ok
```

说明新项目已经成功连接旧生产数据库。

---

## 8. 旧数据库关系数据评估

### 8.1 主要业务模型

通过 Django 模型检查确认：

```text
files.Accession   -> accession
files.Assembly    -> assembly
files.Annotation  -> annotation
```

项目中存在较多外键，例如：

```python
species = models.ForeignKey(...)
accession = models.ForeignKey(...)
project = models.ForeignKey(...)
assembly = models.ForeignKey(...)
annotation = models.ForeignKey(...)
```

同时存在：

```text
status
version
release_version
```

等业务字段。

### 8.2 scan_files 对已有关系的依赖

查看 `scan_files.py` 后发现，其逻辑不是完全从文件重新创建所有业务对象，而是先查找数据库中已有对象。

例如：

```python
accession = Accession.objects.filter(
    accession=accession_code
).first()
```

然后：

```python
assembly = accession.default_assembly or accession.assemblies.first()
```

对于 annotation：

```python
annotation = assembly.default_annotation or assembly.annotations.first()
```

若找不到 accession，则会进入未映射结果，例如：

```text
no_related_object
```

因此，如果直接建立空数据库，仅执行：

```bash
python manage.py migrate
python manage.py scan_files
```

无法保证完整恢复原有：

- Accession
- Assembly
- Annotation
- 默认 Assembly
- 默认 Annotation
- 版本与状态信息
- 外键关系

---

## 9. Accession / Assembly / Annotation 关系备份

### 9.1 导出命令

在旧项目中执行：

```bash
python manage.py dumpdata   files.Accession   files.Assembly   files.Annotation   --indent 2   --output accession_relations.json
```

导出成功。

### 9.2 备份文件

文件位置：

```text
/home/labuser/rdcheng/gd/django2_20260721/accession_relations.json
```

文件大小约：

```text
256K
```

检查：

```bash
head -n 10 accession_relations.json
```

可看到：

```json
{
  "model": "files.accession",
  "pk": 1,
  "fields": {
    ...
  }
}
```

说明导出成功。

### 9.3 该文件的备份范围

`accession_relations.json` 属于局部业务数据备份，主要保存：

- Accession 记录
- Assembly 记录
- Annotation 记录
- 主键
- 外键关系
- 模型中的状态、版本和默认标记等字段

该文件不等同于 MySQL 完整数据库备份，不包含：

- 整个 MySQL 实例
- 所有数据库表
- MySQL 用户与权限
- 文件系统中的实际数据文件
- 未导出的 Django 模型

---

## 10. Django 迁移状态检查

执行：

```bash
python manage.py showmigrations
```

其中 `files` 应用显示：

```text
[X] 0001_initial
[X] 0002_auto_20250701_2239
[X] 0003_alter_genomefile_category
[X] 0004_alter_genomefile_category
[X] 0005_alter_genomefile_category
[X] 0006_tablecolumnconfig
[X] 0007_auto_20260320_1553
[X] 0008_genomefile_accession
[X] 0009_assembly_annotation_genomefile_links
[X] 0010_backfill_default_assembly_annotation
[X] 0011_accession_country_region
[X] 0012_alter_genomefile_category_variableblocks
[X] 0013_accession_genetic_stock_id_assembly_metadata
[X] 0014_assembly_annotation_display_metadata
[ ] 0015_auto_20260602_1057
```

含义：

- `[X]`：迁移已经执行
- `[ ]`：迁移文件存在，但尚未应用到当前数据库

因此当前数据库已经应用 `0001` 到 `0014`，尚缺：

```text
files.0015_auto_20260602_1057
```

在正式执行前，可先检查：

```bash
python manage.py sqlmigrate files 0015
```

或查看迁移源码：

```bash
sed -n '1,240p' files/migrations/0015_auto_20260602_1057.py
```

确认不存在危险的 `DeleteModel`、`RemoveField` 或不符合预期的数据修改后，再运行：

```bash
python manage.py migrate
```

---

## 11. 当前重新部署再次出现的日志路径问题

重新部署过程中再次执行：

```bash
python manage.py makemigrations
```

出现：

```text
FileNotFoundError:
No such file or directory:
/home/labuser/rdcheng/gd/django/logs/filemanager.log
```

并进一步报：

```text
ValueError: Unable to configure handler 'file'
```

最终确认 `settings_production.py` 仍残留旧绝对路径：

```python
'filename': '/home/labuser/rdcheng/gd/django/logs/filemanager.log',
```

当前项目目录实际为：

```text
/home/labuser/rdcheng/gd/django2
```

因此必须修改为：

```python
'filename': '/home/labuser/rdcheng/gd/django2/logs/filemanager.log',
```

并确认：

```bash
mkdir -p /home/labuser/rdcheng/gd/django2/logs
touch /home/labuser/rdcheng/gd/django2/logs/filemanager.log
```

---

## 12. 环境注意事项

曾在 `base` 环境下执行：

```bash
python manage.py shell
```

出现：

```text
ModuleNotFoundError: No module named 'django'
```

原因是没有激活项目环境。

正确操作：

```bash
conda activate django2_env
```

提示符应显示：

```text
(django2_env)
```

再执行 Django 命令。

另外运行过程中多次看到：

```text
pkg_resources is deprecated
```

这属于依赖弃用警告，目前不会阻止 Django 项目运行，可后续统一处理依赖版本。

---

## 13. 当前推荐部署顺序

在继续部署时使用以下顺序：

```text
1. 激活 django2_env
        ↓
2. 确认 settings_production.py 日志路径
        ↓
3. 确认 logs/filemanager.log 存在且可写
        ↓
4. 确认数据库配置为 127.0.0.1:3307
        ↓
5. python manage.py check
        ↓
6. python manage.py showmigrations
        ↓
7. 检查未执行迁移内容
        ↓
8. python manage.py migrate
        ↓
9. python manage.py scan_files
        ↓
10. 启动 Django 服务
```

启动命令：

```bash
python manage.py runserver 0.0.0.0:2025
```

---

## 14. 当前状态总结

| 检查项 | 状态 |
|---|---|
| `django2_env` 环境 | 已确认 |
| Django 可正常加载 | 已确认 |
| 新项目日志目录 | 需要每次部署确认 |
| `filemanager.log` 写权限 | 已验证可修复 |
| 旧 MySQL 数据库 | 已找到 |
| Docker MySQL 容器 | `xbzhang-mysql57` |
| 数据库地址 | `127.0.0.1:3307` |
| 数据库名 | `gene_manage` |
| 新项目数据库连接 | 已验证成功 |
| Accession 关系备份 | 已完成 |
| Assembly 关系备份 | 已完成 |
| Annotation 关系备份 | 已完成 |
| `accession_relations.json` | 已生成 |
| `files` 0001–0014 | 已迁移 |
| `files` 0015 | 待执行/待确认 |
| `scan_files` | 待最终执行 |
| 项目启动 | 待最终验证 |

---

## 15. 本次问题总结

本次 Django2 重新部署中主要出现了三类问题：

### 15.1 日志绝对路径问题

项目迁移或目录改名后，`settings_production.py` 中仍保留旧绝对路径，导致 Django 在启动阶段初始化 `FileHandler` 失败。

典型报错：

```text
ValueError: Unable to configure handler 'file'
```

和：

```text
FileNotFoundError
```

### 15.2 数据库连接目标错误

新项目初始配置连接：

```text
localhost:3306
```

而旧生产数据库实际位于 Docker MySQL：

```text
xbzhang-mysql57
```

宿主机访问地址：

```text
127.0.0.1:3307
```

因此出现：

```text
Unknown database 'gene_manage'
```

修改为旧生产配置后，数据库连接恢复正常。

### 15.3 旧业务关系不能简单通过 scan_files 重建

`scan_files` 会依赖已有的：

```text
Accession
Assembly
Annotation
```

对象及其关系。

因此在考虑新建空数据库前，必须先保护这些业务数据。

本次已经通过 Django `dumpdata` 成功生成：

```text
accession_relations.json
```

作为核心业务关系的独立备份。

---

## 16. 后续建议

1. 将数据库密码从代码中移出，使用环境变量或 `.env` 管理。
2. 日志路径使用 `BASE_DIR` 动态生成，避免再次因目录名变化报错。
3. 正式执行 `migrate` 前保留完整数据库备份。
4. 保留 `accession_relations.json` 作为核心关系的额外恢复手段。
5. 不要执行会删除 Docker volume 的操作，例如：

```bash
docker compose down -v
docker rm -v
```

6. 不要直接删除或修改 MySQL 数据目录。
7. 部署后记录当前 Docker 容器名、端口、数据库用户和配置文件位置，避免下一次部署重新排查。

---

## 17. 实验结论

本次排查确认：

- Django 代码本身可以正常加载。
- 日志错误来自绝对路径失效，而非 Django 数据库命令本身。
- 原生产数据库仍可正常访问。
- 原数据库位于 `xbzhang-mysql57` MySQL 5.7 Docker 容器中。
- Django 通过 `127.0.0.1:3307` 连接该数据库。
- 新项目已验证能够连接旧数据库。
- 原 Accession、Assembly、Annotation 业务关系已通过 JSON 成功备份。
- 当前后续重点是确认 `0015` 迁移内容、执行 `migrate`、运行 `scan_files` 并完成最终启动验证。

